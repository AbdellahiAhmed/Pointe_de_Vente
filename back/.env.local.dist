# =====================================================================
# PRODUCTION ENVIRONMENT TEMPLATE
# Copy this file to .env.local and fill in your values
# DO NOT commit .env.local to version control
# =====================================================================

# Application environment (prod for production)
APP_ENV=prod

# Application secret — generate with: php -r "echo bin2hex(random_bytes(16));"
APP_SECRET=CHANGE_ME_GENERATE_A_RANDOM_STRING

# Activation code for initial setup — choose a secure code
APP_ACTIVATION_CODE=CHANGE_ME_SECURE_CODE

# Database connection
# Format: mysql://user:password@host:port/dbname?serverVersion=mariadb-10.6.0
DATABASE_URL="mysql://pos_user:CHANGE_ME_DB_PASSWORD@127.0.0.1:3306/pos_db?serverVersion=mariadb-10.6.0"

# JWT Authentication
# Generate keys: php bin/console lexik:jwt:generate-keypair
JWT_PASSPHRASE=CHANGE_ME_JWT_PASSPHRASE

# CORS — adjust to match your frontend URL
CORS_ALLOW_ORIGIN='^https?://(localhost|127\.0\.0\.1|your-domain\.com)(:[0-9]+)?$'

# Mailer (optional — for password reset emails)
# MAILER_DSN=smtp://user:password@smtp.example.com:587
